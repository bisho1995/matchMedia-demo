// require("babel-register")  This if used will transpile whatever is after this
// So for example if I use latest js syntax in express.js file this will
// transpile that automatically 
require("./express")